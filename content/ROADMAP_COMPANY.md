# 🗺️ خريطة الطريق ودليل المعمارية لبناء سيستم الشركة المشتركة (Company Portal Roadmap & Architecture)

> **ملاحظة**: هذا الملف هو المرجع الأساسي الموحد الشامل لخريطة طريق ومعمارية **سيستم ولوحة تحكم الشركة المشتركة (Company Portal System)** في منصة **"شحنتك" (Shahntak)**، يجمع بين البنية البرمجية لعزل المستأجرين (Multi-Tenant SaaS Architecture)، الكود المرجعي، بنية الملفات، وخطة بناء الأقسام الستة.

---

## 📌 🎯 حالة سيستم بوابة الشركة (Company Portal Status)

> [!IMPORTANT]
> **إنجاز مكتمل بالكامل 100% (بناء إنتاج ناجح `npm run build` خالي من الأخطاء)**:
> * **تم بناء وتنميط واجهات البوابة الـ 9 بالكامل وربطها بالـ Hooks والـ APIs المعزولة**:
>   1. 📊 **الداشبورد الرئيسية** (`dashboard`)
>   2. 📦 **قسم الطلبات والتجميع** (`orders`)
>   3. 🚚 **قسم الشحنات وطباعة البوالص** (`shipments`)
>   4. 🧾 **قسم الفواتير والفوترة الضريبية وسجل المصروفات** (`invoices`)
>   5. 🗺️ **قسم إدارة المسارات والخطوط اللوجستية** (`routes`)
>   6. 👥 **قسم الموظفين وفريق العمل** (`employees`)
>   7. 🚛 **قسم الأسطول والمركبات** (`vehicles`)
>   8. 📈 **لوحة التقارير والتحليلات الشاملة بالأربعة تبويبات** (`analytics & reports`)
>   9. ⚙️ **قسم إعدادات الشركة والاشتراك** (`settings`)

---

## ✅ حالة إنجاز المراحل (Phases Completion Status)

* ✅ **المرحلة الأولى: التأسيس التقني المعزول وبنية الخدمات وبك-إند الشركة بالكامل (100%)**:
  * **تطوير مسارات الـ Backend المعزولة بـ `companyId`**:
    - **الموظفين (`CompanyUser`)**: `GET`, `POST /new`, `GET/PUT/DELETE /[id]`.
    - **المركبات (`Vehicle`)**: `GET`, `POST /new`, `GET/PUT/DELETE /[id]`.
    - **المسارات والخطوط (`Route`)**: `GET`, `POST /new`, `GET/PUT/DELETE /[id]`.
    - **الطلبات (`Orders`)**: `GET`, `POST /new`, `GET/PUT/DELETE /[id]`.
    - **الشحنات (`Shipments`)**: `GET`, `POST /new`, `GET/PUT/DELETE /[id]`.
    - **الفواتير (`Invoices`)**: `GET`, `POST /new`, `GET/PUT/DELETE /[id]`.
    - **المصروفات التشغيلية (`Expenses`)**: `GET`, `POST`, `PUT /[id]`, `DELETE /[id]`.
    - **التقارير والتحليلات الموزعة (`Analytics`)**: `GET /api/company/analytics?tab=...`.
  * تشمل التأسيس التقني والأنواع وحماية `withCompanyGuard` و `checkSubscriptionQuota` مع خلو الكود من الإيموجيات وتأكيد السلامة بـ `npx tsc --noEmit` وبناء الإنتاج `npm run build` بنسبة 100%.
* ✅ **المرحلة الثانية: التخطيط والهيكل الرئيسي لبوابة الشركة (Company Portal Layout) (100%)**:
  * إنشاء مكون الشريط الجانبي `CompanySidebar.tsx` بتصميم مطابق لـ `AdminSidebar`.
  * إنشاء مكون الهيدر العلوي `CompanyHeader.tsx` بتصميم مطابق لـ `AdminHeader`.
  * إنشاء التخطيط الشامل `CompanyClientLayout.tsx` وتجميع لوحة التحكم `app/company/dashboard/layout.tsx`.
* ✅ **المرحلة الثالثة: بناء وتنميط واجهات بوابة الشركة (مكتملة بالكامل 100%)**:
  * ✅ **الداشبورد الرئيسية للشركة** (`dashboard`): `CompanyDashboard.tsx` واللوحة الإحصائية الحية.
  * ✅ **قسم الطلبات** (`orders`): `CompanyOrders.tsx` ونماذج الإضافة والتعديل والتفاصيل والتجميع.
  * ✅ **قسم الشحنات وتعيين الموارد** (`shipments`): `CompanyShipments.tsx` ونماذج التفاصيل والتتبع وطباعة البولص 🖨️.
  * ✅ **قسم الفواتير وسجل المصروفات** (`invoices`): `CompanyInvoices.tsx` وتطبيق التبويبات الفاصلة بين الإيرادات والمصروفات وحساب صافي الربح.
  * ✅ **قسم إدارة المسارات والخطوط اللوجستية** (`routes`): `CompanyRoutes.tsx` مع الربط المباشر بجدول الناقلين وقوائم الاختيار المنسدلة الخيارية في الشحنات والتجميع.
  * ✅ **قسم فريق العمل والموظفين** (`employees`): `CompanyEmployees.tsx` ونماذج الإضافة والتعديل والتفاصيل والأدوار.
  * ✅ **قسم الأسطول والمركبات** (`vehicles`): `CompanyVehicles.tsx` ونماذج الإضافة والتعديل والتفاصيل.
  * ✅ **لوحة التقارير والتحليلات الشاملة بأربعة تبويبات** (`analytics`): `CompanyAnalyticsDashboard.tsx` والتحميل الكسول وحساب صافي الربح والضريبة ومخططات Recharts.
  * ✅ **قسم إعدادات الشركة والاشتراك** (`settings`): `CompanySettings.tsx` وإدارة ملف الشركة، حماية الحقول الرسمية، وحوكمة ضريبة القيمة المضافة (ZATCA 15% / 0%) وسجل التغييرات (`taxRateAuditLog`).
* ✅ **المرحلة الرابعة: التنعيم البصري والرسوم البيانية وإصلاحات الاستقرار (Visual Refinements & Recharts Integration) (100%)**:
  * 📊 **دمج Recharts (`recharts`)**: بناء الرسوم البيانية التفاعلية (Line, Bar, Doughnut PieChart) بلون الهوية الأساسي `accent`.
  * 🎨 **توحيد المظهر البصري**: تطبيق الهوية الدائرية الموحدة لأيقونات كروت الـ KPI (`w-10 h-10 rounded-full bg-accent/10 text-accent`) والأزرار الفلات في جميع الأقسام.
* ✅ **المرحلة الخامسة: نظام إدارة المصروفات (Expenses System) وفصل المالية (100%)**:
  * 💸 **موديل ومسارات المصروفات**: `models/expense.ts`, `lib/validations/expense.schema.ts`, `/api/company/expenses`.
  * 📝 **تصنيفات المصروفات**: خيارات جاهزة (وقود، صيانة، رواتب، إلخ) مع إمكانية **كتابة تصنيف مخصص يدوياً**.
  * 🧮 **حساب صافي الربح**: حساب تلقائي ($\text{إجمالي الإيرادات} - \text{إجمالي المصروفات} = \text{صافي الربح}$).
  * 🔒 **إلزامية اختيار الشحنة**: تعديل `AddCompanyInvoicePopup.tsx` لجعل اختيار الشحنة إجبارياً لإلغاء خلط الفواتير مع المصروفات.
* ✅ **المرحلة السادسة: لوحة التقارير والتحليلات الشاملة بأربعة تبويبات والتحميل الكسول (100%)**:
  * ⚡ **التحميل التدريجي الكسول (Lazy Loading)**: جلب المؤشرات الكلية فوراً، وجلب بيانات التبويب النشط فقط عند ضغطه لمنع ثقل الواجهة.
  * 💀 **الهيكل العظمي الوهمي (`CompanyAnalyticsSkeleton.tsx`)**: عرض بطاقات ورسومات متحركة أثناء تحميل البيانات.
  * 📊 **التبويبات الأربعة المتخصصة**:
    1. 📊 **المالي (Financial)**: التحصيلات والفواتير المحصلة والمعلقة والمتأخرة ورسم نمو الإيرادات.
    2. 🚛 **التشغيلي (Operational)**: عدد الشحنات، متوسط ربحية وإيراد الرحلة، ورسم نمو الرحلات.
    3. 💸 **المصروفات (Expenses)**: أعلى بند نفقات، متوسط مصروف الرحلة، والرسم الدائري لتوزيع المصروفات.
    4. 🏛️ **الضريبي (ZATCA Tax)**: ضريبة المبيعات الصادرة، ضريبة المدخلات، وصافي الضريبة المستحقة للهيئة.
* ✅ **المرحلة السابعة: تحسين وتطهير تجربة المدخلات وتفرغ النماذج (Form Cleanups & Auto Reset) (100%)**:
  * 🧹 **إزالة القيم الافتراضية الثابتة**: تنظيف النماذج من أي نصوص أو أسعار افتراضية ثابتة والاعتماد الكلي على Placeholders توضيحية.
  * 🔄 **التفريغ التلقائي للنماذج (Auto Form Reset)**: إعادة تصفير حالة النماذج فور نجاح أي عملية إضافة في كافة الأقسام.
  * 🗺️ **الربط الخياري للمسارات بالشحنات**: إتاحة اختيار مسار مسجّل أو إدخال مسار مخصص يدوياً مع التعبئة التلقائية.

---

## 📌 🛑 نقطة التوقف الحالية والخطوة القادمة المباشرة (Current Stop Point & Next Task)

> **ملاحظة هامة للمساعد الذكي AI**:
> **وقفنا في هذه الجلسة عند**: إتمام وتأكيد **نظام المسارات اللوجستية وتفريغ النماذج تلقائياً والتحقق من الإنتاج بنسبة 100% (`npm run build` ناجح كلياً)**.
>
> **الخطوة القادمة المباشرة المقرر تنفيذها (الطباعة)**:
> 🖨️ **تخصيص وتطوير قوالب طباعة الفواتير الضريبية والبوالص**:
> 1. تحسين تنسيقات الطباعة للورق (`@media print` CSS) للبوالص والفواتير المالية لإبراز الخصم والأنصاب الضريبية والإقرار الرسمي.

---

## 🎯 1. نظرة عامة والأهداف المعمارية

سيستم الشركة المشتركة هو البوابة المخصصة لشركات الشحن والشركاء اللوجستيين المستأجرين (Tenants) لإدارة عملياتهم اليومية المستقلة:
* **العزل التام للبيانات (Strict Data Isolation)**: عزل استعلامات وقواعد بيانات كل شركة حصرياً بناءً على `session.user.companyId`.
* **أتمتة فحص الصلاحيات والحصص (Quota & Subscription Enforcement)**: ربط عمليات الإضافة والتعديل تلقائياً بالباقة السحابية النشطة للشركة والحدود الشهرية المسموحة.
* **التوافق البصري التام (Design System Alignment)**: تطبيق المعايير الدقيقة المعتمدة في [`content/ADMIN_UI_DESIGN_SYSTEM.md`](file:///e:/projects/shahntak/content/ADMIN_UI_DESIGN_SYSTEM.md) و [`content/ADMIN_SECTIONS_STANDARDIZED_GUIDE.md`](file:///e:/projects/shahntak/content/ADMIN_SECTIONS_STANDARDIZED_GUIDE.md).

---

## 🛠️ 2. البنية البرمجية والمجالات المعزولة المكتملة

```text
shahntak/
├── models/
│   ├── invoice.ts                           # موديل الفواتير وتجميد الضريبة taxRateSnapshot والخصم discount
│   └── expense.ts                           # موديل المصروفات التشغيلية والمستندات
├── lib/
│   └── validations/
│       ├── tax.schema.ts                    # مخطط ZATCA للضريبة (15% / 0%) والسبب
│       └── expense.schema.ts                # مخطط Zod للمصروفات والتصنيفات المخصصة
├── services/
│   └── company/
│       ├── CompanyInvoiceServices.ts        # خدمات الفواتير
│       ├── CompanyExpenseServices.ts        # خدمات المصروفات
│       └── CompanyAnalyticsServices.ts      # خدمات التحليلات الموزعة
├── hooks/
│   └── company/
│       ├── useCompanyInvoice.ts             # هوك الفواتير وتصفية التاريخ
│       ├── useCompanyExpenses.ts            # هوك المصروفات وتصفية التاريخ والتصنيفات
│       └── useCompanyAnalytics.ts           # هوك التحليلات الكسول المقسم بالحالة
├── app/
│   └── api/
│       └── company/
│           ├── invoices/                    # APIs الفواتير وإنشاء الفاتورة وتجميد الضريبة
│           ├── expenses/                    # APIs المصروفات والتصفية والتعديل والحذف
│           └── analytics/                   # APIs التحليلات والتبويبات الأربعة
└── components/
    └── company/
        ├── invoices/                        # مكونات الفواتير، والمصروفات، ونوافذ الإضافة والتعديل
        └── analytics/                       # لوحة التحليلات الشاملة components/company/analytics/
            ├── CompanyAnalyticsDashboard.tsx
            └── CompanyAnalyticsSkeleton.tsx
```
